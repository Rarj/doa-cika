<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="skripsi.cika.doaharianpaud">

  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.DoaHarianPaud">
    <activity android:name=".uploadtugas.UploadTugasActivity"></activity>
    <activity android:name=".student.StudentActivity" />
    <activity android:name=".inputnilai.InputNilaiMuridActivity" />
    <activity android:name=".register.RegisterUserActivity" />
    <activity android:name=".MainActivity" />
    <activity android:name=".detaildoa.DetailDoaActivity" />
    <activity android:name=".admin.AdminMainActivity" />
    <activity android:name=".login.LoginActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>